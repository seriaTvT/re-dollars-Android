<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark splash background: black in dark mode (light-pink splash lives in the
         default values/colors.xml). Keeps the cold-start window from flashing white. -->
    <color name="splash_background">#000000</color>

    <!-- Dark post-splash window background: a near-black M3 surface so the splash→chat
         handoff stays dark instead of flashing white. -->
    <color name="window_background">#141218</color>
</resources>
